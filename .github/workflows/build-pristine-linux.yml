name: Build pristine opencode Linux

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Official anomalyco/opencode tag to build
        required: true
        default: v1.14.48

permissions:
  contents: read

jobs:
  build-linux:
    name: Build Linux CLI
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout official opencode
        uses: actions/checkout@v4
        with:
          repository: anomalyco/opencode
          ref: ${{ inputs.tag }}
          fetch-depth: 0

      - name: Verify pristine source
        shell: bash
        run: |
          test "$(git remote get-url origin)" = "https://github.com/anomalyco/opencode"
          git describe --tags --exact-match

      - uses: ./.github/actions/setup-bun

      - name: Set version
        shell: bash
        run: |
          echo "OPENCODE_VERSION=${TAG#v}" >> "$GITHUB_ENV"
          echo "OPENCODE_CHANNEL=latest" >> "$GITHUB_ENV"
        env:
          TAG: ${{ inputs.tag }}

      - name: Build Linux x64 CLI binary
        run: ./packages/opencode/script/build.ts --single

      - name: Verify Linux x64 binary
        shell: bash
        run: packages/opencode/dist/opencode-linux-x64/bin/opencode --version

      - name: List artifact files
        shell: bash
        run: find packages/opencode/dist/opencode-linux-x64 -type f -print

      - name: Upload Linux x64 binary
        uses: actions/upload-artifact@v4
        with:
          name: opencode-linux-x64-${{ inputs.tag }}
          path: |
            packages/opencode/dist/opencode-linux-x64/bin/opencode
            packages/opencode/dist/opencode-linux-x64/package.json
